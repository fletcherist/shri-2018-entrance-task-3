import { h } from 'preact'
import { connect } from 'preact-fela'

const style = state => ({
  
})

const DateSwitcher = () => (
  <div>
    hello worodl
  </div>
)

export default connect({style})(DateSwitcher)